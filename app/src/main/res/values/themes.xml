<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- use a MaterialComponents parent -->
    <style name="Theme.ResilientP2PTestbed" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Launch Screen Theme only. Actual app theme via Compose. -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>

